{
  "name": "genomeatlas-site-helper-suite",
  "private": true,
  "type": "module",
  "scripts": {"test": "node --test tests/*.test.mjs", "serve": "python -m http.server 8765 --bind 127.0.0.1 --directory docs"}
}
