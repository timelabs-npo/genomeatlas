# Independently accepted Site Helper Suite candidate

Original MIT code by timelabs-npo. Local Codex built this suite on the authorized Windows endpoint. The parent independently reran 18 Node contract tests, 11 Python tests (one invokes the same Node suite), 13 Copilot receipt tests, two data checks, and nine Chrome UI checks on desktop/mobile. Test definitions are not double-counted. Ubuntu separately passed the data and invariant checks; this was WSL1, not WSL2.

Copilot PR4 payloads were selectively imported from 388cc5e60bb16f1f4a6fb7e4b47239a1120b2999; compiled pyc artifacts were excluded. Imported receipt validity is not execution authentication. No full genome workflow, biological finding, GPU inference, or native Sites deployment is claimed.

Native Site shell/account access exists, but publication is not accepted. smarts.bio catalog/workspace probes succeeded; its advertised GC tool returned HTTP404. BioNeMo source/skill inspection succeeded; model execution remains NOT_RUN.

Actual command outputs and browser checks are in acceptance/. The frozen 177 accession list is included. Private transcripts, machine identifiers and authentication data are not published.
