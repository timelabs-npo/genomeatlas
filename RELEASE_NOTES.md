# GenomeAtlas v0.1.0 — Site Helper Suite
Original product code is MIT, timelabs-npo. This release is the helper surface, not a completed genome analysis.
Contents: 63 tool/service/skill-package entries, 177 frozen RefSeq IDs, 8 proposed chains, coordinate-convention checker, safe schema-bound JSON previews, task-request creation and exports. Local confirmation cannot execute a job or manufacture acceptance.
Actual parent rechecks: Windows/Ubuntu bidirectional file exchange, authenticated GitHub roundtrip, one NCBI assembly metadata HTTP200, and 11 Copilot inventory tests on exact PR27 commit. These are not full-genome runs.
Codex implemented the source locally on WD. Parent reran 22 Python and 13 Node tests. Actual Edge/localhost rendered the page and passed nine interaction checks after fixing Windows .mjs MIME. The complete browser suite is NOT accepted: the parent harness subsequently inspected hidden collapsed text incorrectly, and further checks stopped after a delegated browser-permission denial became known.
Native ChatGPT Sites metadata was reached through a separate actual WD Codex session; existing GenomeAtlas owner-only project was reused. No version or deployment exists. A slug write was refused by the noninteractive approval policy. A GitHub Pages URL must not be called a native ChatGPT Site.
smarts.bio catalog/workspace discovery succeeded, but the advertised sequence-statistics tool returned 404. BioNeMo Agent Toolkit v1.1.0 guidance was read; no GPU inference or CPU/GPU parity was run. AlphaGenome contributes coordinate/configuration concepts only, not LAB predictions.
Historical source receipts are preserved as source claims. Dated parent updates are in data/live_probes.json. This repo contains no user credentials, raw private manuscripts, or model-provider keys.
Verify: python scripts/build.py; python -m unittest discover -s tests -v; node --test tests/*.test.js. Live biological stages and native deployment remain NOT_EXECUTED.