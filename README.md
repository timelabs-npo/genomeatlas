# GenomeAtlas — Site Helper Suite

A timelabs-npo MIT-licensed helper for evidence-gated LAB phylogenomics, available tools, actual probe receipts, cross-device navigation, and bounded human-confirmed delegation.

**Native ChatGPT Site:** https://genomeatlas-site-helper-suite.hx31337.chatgpt.site/

The provider and unauthenticated HTTP200 verified an existing public Site. Saved version/source and limits are in data/native-site-readback.json. Current source may be newer than the deployed version; no automatic equation between main and live content.

## Use and test

Requires Node20+ and Python3. No dependency installation or secrets required for this static suite.

```sh
python scripts/build.py
node --test tests/*.test.js
python scripts/serve.py
```

Use python3 on Linux. Browser permission was declined in this session, so no browser acceptance is claimed.

## Evidence boundaries

134 registry records =63 existing catalog entries +71 actual cached plugin manifests with344 skill-file records. Cached is not enabled, authenticated or runnable. smarts.bio requires login in the parent chat; BioNeMo skills are installed/read but no NIM job ran. Original code is MIT; third-party software/skills retain their own licenses.

The177 frozen RefSeq identifiers remain metadata-unrefreshed. Host topology must use conserved proteins, independent of R-M annotations. Failed or unreviewed data must not be encoded as absence. The site creates task requests, not execution or scientific certification.

See docs/RECONCILIATION_20260909.md and exported JSON receipts.
